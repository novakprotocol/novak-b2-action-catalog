# Next Actions - Action Catalog

## Immediate next code

```text
adapter-contract-hardening-v2
```

## Hard next-12

1. Add strict schema checks for scaled AI JSONL action contracts.
2. Add strict schema checks for AWX candidates.
3. Add strict schema checks for AWS SSM candidates.
4. Add strict schema checks for Kubernetes/OpenShift candidates.
5. Add deterministic artifact regeneration check.
6. Add duplicate action-id check across all scaled adapters.
7. Add no-mutation keyword denylist for scaled adapter outputs.
8. Add no-live-endpoint and no-credential-reference checks.
9. Add size/readability split plan for huge generated artifacts.
10. Add docs explaining consumer boundaries by platform.
11. Add CI wiring for scaleout validator if not already wired.
12. Re-tag a hardened release floor after merge.

## Rule

The Action Catalog has enough volume. The next value is trust, shape, consumption, and validator depth.